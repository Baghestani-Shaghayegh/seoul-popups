export declare const colors: {
  brand: { DEFAULT: string; dark: string; light: string };
  purple: { DEFAULT: string; light: string };
  bg: string;
  surface: string;
  well: string;
  ink: string;
  muted: string;
  faint: string;
  line: { DEFAULT: string; strong: string };
  peach: { DEFAULT: string; ink: string };
};
